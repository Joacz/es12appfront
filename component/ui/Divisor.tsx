'use client';
import { FC } from 'react';

export const Divisor: FC = ({}) => {
  return (
    <div
      style={{
        height: '5px',
        background: '#9ed4c7',
        width: '50%',
        marginBottom: '2em',
      }}
    ></div>
  );
};
