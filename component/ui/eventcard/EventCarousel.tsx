'use client';
import { FC } from 'react';

interface EventCarouselProps {
  children: JSX.Element | JSX.Element[];
}

export const EventCarousel: FC<EventCarouselProps> = ({ children }) => {
  return <></>;
};
